require 'spec_helper'

describe Spectrate do
  it 'has a version number' do
    expect(Spectrate::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
